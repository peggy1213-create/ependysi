# Copy to frontend/.env for local overrides.

# API origin for production builds. Leave blank in dev (Vite proxies /api).
VITE_API_BASE_URL=

# Override the backend target the Vite dev-server proxy points at.
VITE_API_PROXY_TARGET=http://localhost:4000
